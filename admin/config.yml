backend:
  name: github
  repo: YOUR_USERNAME/bike-tour-planner
  branch: main

collections:
  - name: "stops"
    label: "Stops"
    files:
      - file: "data/stops.json"
        label: "Stops Liste"
        name: "stops"
        format: "json"
